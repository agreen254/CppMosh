#include <iostream>

using namespace std;

int main() 
{
    // A pointer is a special variable that holds the address of another variable in memory.
    // ex: number 10 is stored at address 0x7ffeec
    // we declare some pointer ptr that holds this address 0x7ffeec

    // Reasons we use pointers:
    // efficiently passing large objects between functions; better to pass by ref than copy; use ref parameters/ref variables/pointers
    // dynamic memory allocation; allow programs to adjust memory usage depending on input
    // enabling polymorphism; 

    return 0;
}